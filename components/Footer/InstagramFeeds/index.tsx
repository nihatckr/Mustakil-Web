import { Heading } from '@chakra-ui/react';
import React from 'react';

const InstagramFeeds = ({ feed }: any) => {
  console.log(feed);
  return <Heading>Instagram Feeds</Heading>;
};

export default InstagramFeeds;
